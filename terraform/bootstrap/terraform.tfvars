# S3 bucket that stores Terraform state
state_bucket_name = "bankit-cloud-terraform-state"

# Optional, only if you use a DynamoDB lock table in bootstrap
# state_lock_table_name = "bankit-cloud-terraform-locks"

region = "us-east-1"
